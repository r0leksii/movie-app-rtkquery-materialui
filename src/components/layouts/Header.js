import { SearchBar } from '../common/SearchBar'
import { GenresBar } from '../common/GenresBar'

export const Header = () => {
  return (
    <header>
      <SearchBar />
      <GenresBar />
    </header>
  )
}
